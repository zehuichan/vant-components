import {{UpperCamelCase name}} from './{{ dashCase name }}'

{{UpperCamelCase name}}.install = function (Vue) {
  Vue.component({{UpperCamelCase name}}.name, {{UpperCamelCase name}})
}

export default {{UpperCamelCase name}}
